// Action types

// Initial state

const initialState = {};

// Reducer

export const rooterReducer = (state = initialState, { type, payload }) => {
  switch (type) {
    default:
      return state;
  }
};
